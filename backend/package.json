{
  "name": "@wishlive/backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test --max-warnings=0",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@wishlive/shared": "workspace:*",
    "ai": "6.0.197",
    "redis": "6.0.0",
    "zod": "4.1.13"
  },
  "devDependencies": {
    "vitest": "4.0.14"
  }
}
